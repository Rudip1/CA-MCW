from setuptools import setup

package_name = "vf_robot_messages"

setup(
    name=package_name,
    version="0.0.1",
    packages=[],
    py_modules=[],
    install_requires=["setuptools"],
    zip_safe=True,
)
