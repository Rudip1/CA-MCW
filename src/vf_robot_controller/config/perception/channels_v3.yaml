# channels_v3.yaml — full system. Adds SLAM-derived persistent features.
# Total: ~170 dims. Requires backend with topology + 3D capabilities.

feature_extractor_node:
  ros__parameters:
    enabled_channels:
      - robot_state            # 9
      - context                # 9
      - path_geometry          # 14
      - gcf_rosette            # 48
      - critic_history         # 30
      - obstacle_dynamics      # 16
      - reynolds               # 4
      - slam_persistent        # 40
