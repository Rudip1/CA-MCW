"""
vf_controller — Python sub-package for inference and offline training.

Subpackages:
  inference/  — runs at deploy time inside ROS (metacritic_inference_node.py)
  training/   — pure offline tools (no ROS at runtime)
"""
